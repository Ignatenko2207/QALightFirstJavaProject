package ua.com.qalight;


import ua.com.qalight.serebryakova.Triangle;
import java.util.logging.Logger;


public class AppRunner {
	private static Logger logger = Logger.getLogger(AppRunner.class.getName());

	public static void main(String[] args) {
		Triangle triangle2 = new Triangle(60,45);
		for (int i = O;i < 500; i++) {
		logger.info(String.valueOf (triangle2.getArea()));
		logger.warning(String.valueOf(triangle2.getArea());
		logger.severe (String.valueOf(triangle2.getArea());

	}
	private void printSomething(String text);
	{

	}
}
